# Инструкция по сборке приложения HRLeave

## Проблема: "no usable data found" при сканировании QR-кода

Это происходит потому, что ваше приложение использует **нативные модули**, которые не работают в стандартном Expo Go:
- `expo-speech-recognition` (голосовое управление)
- `expo-face-detector` (распознавание лиц)
- `expo-camera` (камера для Face ID)

## ✅ Решение: Development Client

### Шаг 1: Запустить сборку

В новом окне PowerShell выполните:
```bash
cd Desktop/office-mobile
npx eas build --profile development --platform android
```

**Что произойдет:**
1. EAS спросит подтверждение → нажмите `y` или `Enter`
2. Начнется сборка (5-10 минут)
3. Вы увидите прогресс в терминале
4. После завершения получите ссылку на скачивание APK

### Шаг 2: Установить APK на Android устройство

1. Скачайте APK файл по ссылке из терминала
2. Перенесите на телефон (через USB, email, облако)
3. Установите APK
   - Если система спросит - разрешите установку из неизвестных источников
4. На телефоне появится приложение **"HRLeave (dev)"**

### Шаг 3: Запустить development сервер

```bash
npx expo start --dev-client
```

### Шаг 4: Подключиться

1. Откройте приложение **"HRLeave (dev)"** на телефоне
2. Отсканируйте QR-код из терминала
3. Приложение загрузится со всеми функциями!

## 🎯 После первой сборки

**Хорошая новость:** Сборку нужно делать только ОДИН РАЗ (или когда добавляете новые нативные модули).

Для обычной разработки просто запускайте:
```bash
npx expo start --dev-client
```

## ❓ Когда нужна пересборка?

Пересобирать development client нужно только когда:
- Добавляете новые нативные модули
- Обновляете версию Expo SDK
- Меняете нативные настройки в `app.json`

Обычные изменения кода (TypeScript, компоненты) работают без пересборки!

## 🚀 Альтернатива: EAS Update (для продакшена)

После того как приложение будет готово, можно собрать production версию:
```bash
npx eas build --profile production --platform android
```

## Проверка статуса сборки

В любой момент можно проверить статус:
```bash
npx eas build:list
```

Или в веб-интерфейсе: https://expo.dev/accounts/roman_92/projects/hrleave/builds

## Решение проблем

**Сборка не запускается:**
```bash
npx eas login
npx eas whoami  # Проверить, что вы залогинены
```

**Сборка провалилась:**
- Проверьте логи в терминале
- Или на expo.dev в разделе Builds

**APK не устанавливается:**
- Включите "Установка из неизвестных источников" в настройках Android
- Проверьте, что скачали полный файл

## 📱 Минимальные требования

- **Android:** версия 5.0 (API 21) или выше
- **Интернет:** для первой загрузки приложения
- **Место:** ~50-100 MB для APK

---

**Все исправления уже внесены!** Просто следуйте шагам выше.
