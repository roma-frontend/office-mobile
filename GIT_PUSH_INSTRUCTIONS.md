# 🚀 Инструкция по Push в GitHub

## ✅ Что уже сделано

1. ✅ Удалены все временные файлы (MD документация, скрипты, backup)
2. ✅ Git репозиторий инициализирован
3. ✅ Все файлы добавлены в staging
4. ✅ Создан initial commit с полным описанием

## 📋 Следующие шаги

### Вариант 1: Создать новый репозиторий на GitHub

1. **Откройте GitHub:**
   - Перейдите на https://github.com
   - Войдите в свой аккаунт

2. **Создайте новый репозиторий:**
   - Нажмите "+" в правом верхнем углу
   - Выберите "New repository"
   - Название: `office-mobile` (или любое другое)
   - Описание: "Modern HR management mobile app built with React Native and Expo"
   - Visibility: Public или Private (на выбор)
   - ❌ **НЕ СОЗДАВАЙТЕ** README, .gitignore, license (у нас уже есть)
   - Нажмите "Create repository"

3. **Скопируйте URL репозитория:**
   - GitHub покажет вам URL типа: `https://github.com/username/office-mobile.git`
   - Или SSH: `git@github.com:username/office-mobile.git`

4. **Добавьте remote и запушьте:**

```powershell
cd Desktop/office-mobile

# Добавьте remote (замените URL на свой)
git remote add origin https://github.com/YOUR_USERNAME/office-mobile.git

# Или если используете SSH
git remote add origin git@github.com:YOUR_USERNAME/office-mobile.git

# Проверьте remote
git remote -v

# Запушьте код
git push -u origin master

# Или если ваша основная ветка называется main
git branch -M main
git push -u origin main
```

---

### Вариант 2: Использовать существующий репозиторий

Если у вас уже есть репозиторий:

```powershell
cd Desktop/office-mobile

# Добавьте remote
git remote add origin <YOUR_REPO_URL>

# Запушьте (force, так как это новый проект)
git push -u origin master --force
```

---

## 🔐 Аутентификация GitHub

### Если используете HTTPS:

1. GitHub попросит логин и пароль
2. **Вместо пароля используйте Personal Access Token:**
   - GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token
   - Выберите scopes: `repo` (полный доступ к репозиториям)
   - Скопируйте токен и используйте вместо пароля

### Если используете SSH:

1. Проверьте SSH ключ:
```powershell
cat ~/.ssh/id_rsa.pub
```

2. Если ключа нет, создайте:
```powershell
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

3. Добавьте ключ в GitHub:
   - GitHub Settings → SSH and GPG keys → New SSH key
   - Скопируйте содержимое `~/.ssh/id_rsa.pub`

---

## 📊 Что будет запушено

**73 файла, 28,184 строк кода:**

### Основные директории:
- ✅ `app/` - все экраны приложения (auth, tabs)
- ✅ `components/` - переиспользуемые компоненты
- ✅ `constants/` - темы и конфигурация
- ✅ `context/` - React contexts (Theme, Auth, Toast)
- ✅ `convex/` - backend функции и схема
- ✅ `assets/` - иконки и изображения
- ✅ `lib/` - вспомогательные функции

### Конфигурационные файлы:
- ✅ `package.json` - зависимости
- ✅ `app.json` - Expo конфигурация
- ✅ `eas.json` - EAS Build конфигурация
- ✅ `tsconfig.json` - TypeScript конфигурация
- ✅ `.gitignore` - исключения Git
- ✅ `.env` - переменные окружения
- ✅ `README.md` - документация проекта

---

## ⚠️ Важно

### Проверьте .env перед push:

Файл `.env` содержит чувствительные данные:
- ✅ `EXPO_PUBLIC_*` - можно оставить (публичные)
- ❌ `CLOUDINARY_API_SECRET` - СЕКРЕТНЫЙ!
- ❌ `GROQ_API_KEY` - СЕКРЕТНЫЙ!

**Рекомендация:** 
1. Создайте `.env.example` без секретов
2. Добавьте `.env` в `.gitignore` (уже добавлено)

```powershell
# Проверьте, что .env в .gitignore
cd Desktop/office-mobile
Get-Content .gitignore | Select-String "\.env"
```

---

## ✅ После успешного push

1. **Проверьте репозиторий на GitHub:**
   - Все файлы должны быть видны
   - README.md должен отображаться на главной странице

2. **Добавьте описание проекта:**
   - GitHub репозиторий → About → Edit
   - Добавьте описание, теги, website

3. **Защитите ветку (опционально):**
   - Settings → Branches → Add rule
   - Защитите `main` или `master` от force push

---

## 🆘 Troubleshooting

### Ошибка: "remote origin already exists"
```powershell
git remote remove origin
git remote add origin <NEW_URL>
```

### Ошибка: "Updates were rejected"
```powershell
# Если это действительно новый проект
git push -u origin master --force
```

### Ошибка: Authentication failed
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключ

---

## 📝 Пример полной команды

```powershell
cd Desktop/office-mobile

# Проверяем текущее состояние
git status

# Добавляем remote (замените на свой URL!)
git remote add origin https://github.com/YOUR_USERNAME/office-mobile.git

# Проверяем remote
git remote -v

# Переименовываем ветку в main (если нужно)
git branch -M main

# Пушим код
git push -u origin main
```

---

## 🎉 Готово!

После успешного push ваш проект будет доступен на GitHub!

**URL:** `https://github.com/YOUR_USERNAME/office-mobile`

---

**Дата:** 27 февраля 2026  
**Commit:** Initial commit с 73 файлами
